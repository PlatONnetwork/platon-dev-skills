interface:
  display_name: "Chainlist Query"
  short_description: "Query PlatON chain info from chainlist"
  default_prompt: "Use $platon-chainlist to look up validated PlatON dev or mainnet info from Chainlist."
