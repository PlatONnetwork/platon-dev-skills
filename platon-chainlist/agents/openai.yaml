interface:
  display_name: "Chainlist Query"
  short_description: "Query Chainlist info for PlatON and other EVM chains"
  default_prompt: "Use $platon-chainlist to look up validated Chainlist RPC and explorer info for a requested chain and network."
